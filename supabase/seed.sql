-- LifeOS Database Seed Data
-- Run this in Supabase SQL Editor AFTER schema.sql
-- (Seed data has been removed for production usage. Create users and data through the app.)

DO $$
BEGIN
    RAISE NOTICE 'Seed script executed. (No demo data inserted in production version)';
END $$;
